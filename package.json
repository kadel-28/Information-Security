{
  "name": "algorithm-visualizer",
  "version": "1.0.0",
  "description": "Full-stack MERN Algorithm Visualizer",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "start": "cd server && npm start",
    "dev": "cd server && npm run dev",
    "build": "cd client && npm install && npm run build"
  }
}
